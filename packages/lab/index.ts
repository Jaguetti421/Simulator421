/**
 * @lastclan/lab — library entry point (the CLI lives in ./cli).
 *
 * W0-01 ships the skeleton only. Fixture parser (W0-05), runner and bundles
 * (W0-06) and the readability renderer (W0-09) arrive with their packets.
 */
export const PACKAGE_NAME = "@lastclan/lab" as const;
