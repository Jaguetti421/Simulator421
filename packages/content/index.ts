/**
 * @lastclan/content — package entry point.
 *
 * W0-01 ships the skeleton only. The definitions directory, compiler, validators
 * and catalogs are added by the packets that own them (TP v1.1 §12).
 */
export const PACKAGE_NAME = "@lastclan/content" as const;
