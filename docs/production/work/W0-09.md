# W0-09 — Readability renderer and clanlab render

Phase: **W0 — Web foundation and runtime proof** · Size: **M** · Depends on: W0-06, W0-07

## Purpose and exact scope
Implement the deterministic 2D top-down render of any snapshot (terrain classes, law boundaries, actors as discs with clan ring, headwear glyph, action icon, nameplate) in packages/lab using node-canvas, expose `clanlab render`, and add the first readability assertions.

This is one bounded packet. Implement the capability and its verification; do not implement systems mentioned only by its dependencies. If it will not fit one session, checkpoint, write SESSION_RESUME.md and continue in the next session, or split it into numbered children with unchanged aggregate acceptance.

## Read first
AGENTS.md; state/STATUS.md; then: **TP v2.0 §13, §20; GDD 14; PRESENT 01 (first automated half)**. "TP v2.0" is The_Last_Clan_Technical_Plan_v2_0_Web.md; "TP v1.1" is reference/Technical_Plan_v1_1.md (authoritative for sections v2.0 does not override). "GDD" is design/GDD_v1.md plus design/GDD_v1_1_Addendum.md.

## Observable acceptance
1. `clanlab render --fixture <f> --tick <t> --out <png>` produces a PNG in the sandbox with metadata (fixture, tick, build hash) for the synthetic workload.
2. Readability assertions exist and run: nameplate overlap count below a threshold, clan ring contrast above a threshold, every action-state icon distinct; thresholds are recorded as TUNE.
3. The renderer is pure over the snapshot: same snapshot, same bytes.

## Tests and fixtures to add or extend
- packages/lab/render/*.test.ts
- tests/fixtures/PRESENT-READ-01.json

## In-scope paths
- `packages/lab/render/**`

## Notes
This renderer becomes the in-game overview map later (P1-29). It is evidence for layout and readability, not for 3D quality.

## Return and stop
Commit (push or archive), update state/STATUS.md and state/workboard.json, write handoffs/W0-09/HANDOFF.md from templates/HANDOFF.md, and run the fresh-context self-review (templates/REVIEW.md) before marking ACCEPTED. No unrun check is PASS.
