# W0-07 — Minimal deterministic tick kernel and 137-actor synthetic workload (Node + Worker)

Phase: **W0 — Web foundation and runtime proof** · Size: **M** · Depends on: W0-03, W0-04

## Purpose and exact scope
Implement the ten-stage tick loop skeleton, pause barrier and command queue with a synthetic motion and law-query workload for 136/137 actors, runnable in Node and in a Worker, with operation counters and canonical hashes.

This is one bounded packet. Implement the capability and its verification; do not implement systems mentioned only by its dependencies. If it will not fit one session, checkpoint, write SESSION_RESUME.md and continue in the next session, or split it into numbered children with unchanged aggregate acceptance.

## Read first
AGENTS.md; state/STATUS.md; then: **TP v1.1 §4, §18; TP v2.0 §3–4, §18; G0 contract**. "TP v2.0" is The_Last_Clan_Technical_Plan_v2_0_Web.md; "TP v1.1" is reference/Technical_Plan_v1_1.md (authoritative for sections v2.0 does not override). "GDD" is design/GDD_v1.md plus design/GDD_v1_1_Addendum.md.

## Observable acceptance
1. Same input tape produces equal authoritative hashes across repeated Node runs and between Node (`worker_threads`) and the browser Worker path (browser half verified in W0-10 via Playwright, recorded BLOCKED until then).
2. Pause and tick boundaries are explicit; stage order matches TP v1.1 §4 and is asserted by a fixture.
3. Counters (route-like queries, perception-like queries, actors) and wall-time per tick are reported with the workload's omissions listed; it is labeled synthetic and cannot satisfy FINALE 01.

## Tests and fixtures to add or extend
- tests/fixtures/PERF-OPS-BASE.json
- packages/sim/core/tick.test.ts

## In-scope paths
- `packages/sim/core/**`
- `packages/sim/host/**`
- `tests/fixtures/PERF-OPS-BASE.json`

## Notes
No AI, combat or real spatial claims. Record tick P95 in Node against the TP v2.0 §18 proposed target.

## Return and stop
Commit (push or archive), update state/STATUS.md and state/workboard.json, write handoffs/W0-07/HANDOFF.md from templates/HANDOFF.md, and run the fresh-context self-review (templates/REVIEW.md) before marking ACCEPTED. No unrun check is PASS.
