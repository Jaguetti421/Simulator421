# W0-04 — Contracts v0 and dependency boundaries

Phase: **W0 — Web foundation and runtime proof** · Size: **M** · Depends on: W0-02

## Purpose and exact scope
Define PlayerCommand, CommandAck, ActionRequest/Result, ActorKnowledgeView, IPerceptionQuery, IRouteQuery, RouteResult, DamageProposal, DecisionTrace, CommittedEvent, RenderSnapshot, IStateSectionCodec, ContentProfile and FinalResult as TypeScript types plus JSON schemas, golden samples and the reason-ID registry.

This is one bounded packet. Implement the capability and its verification; do not implement systems mentioned only by its dependencies. If it will not fit one session, checkpoint, write SESSION_RESUME.md and continue in the next session, or split it into numbered children with unchanged aggregate acceptance.

## Read first
AGENTS.md; state/STATUS.md; then: **contracts/INTERFACES.md; TP v1.1 §3, §12; TP v2.0 §3**. "TP v2.0" is The_Last_Clan_Technical_Plan_v2_0_Web.md; "TP v1.1" is reference/Technical_Plan_v1_1.md (authoritative for sections v2.0 does not override). "GDD" is design/GDD_v1.md plus design/GDD_v1_1_Addendum.md.

## Observable acceptance
1. Golden JSON samples in contracts/samples validate against the schemas and round-trip through the codecs; rejection and unknown-version samples are rejected.
2. The architecture test proves ai/ cannot import core/ implementation and nothing in packages/sim imports rendering or DOM.
3. The reason registry contains at least the mandatory v0 IDs from INTERFACES.md, each with player text and required debug fields; contract version and change procedure are recorded in contracts/registry.json.

## Tests and fixtures to add or extend
- packages/sim/contracts/*.test.ts
- tests/arch/boundaries.test.ts (extended)

## In-scope paths
- `packages/sim/contracts/**`
- `contracts/samples/**`
- `contracts/registry.json`

## Notes
Types are immutable records; expose narrow ports, never mutable World access.

## Return and stop
Commit (push or archive), update state/STATUS.md and state/workboard.json, write handoffs/W0-04/HANDOFF.md from templates/HANDOFF.md, and run the fresh-context self-review (templates/REVIEW.md) before marking ACCEPTED. No unrun check is PASS.
