# Independent review — W0-06 / attempt 1

Reviewer / role / fresh-context declaration: same developer, **same session as the author** — this is not a fresh-context review, and it is written before Jani has accepted. The relaxed rule for one-window work applies (session prompt: write the review honestly declaring it is same-session; Jani accepts in chat). W0-05's review, by contrast, genuinely was fresh-context, and the difference is worth keeping visible in the record.
Author / base / commit: same developer; base `cf900fe` (W0-05 ACCEPTED) → `1df45c0` (pushed), plus the host-reporting correction below.
Files actually inspected and checks independently reproduced: the whole diff (`packages/lab/{cli,bundle}`, README, `.gitignore`, STATUS, workboard); `npm run verify` → 0 (build, lint, **350/350** vitest across 18 files, 6/6 python, workboard PASS); the CLI transcript re-run end to end after the correction (`evidence/cli-transcript.txt`); seven mutation checks, each failing and each restored green; `clanlab inspect` run against the **committed** sample bundle in `evidence/bundle-sample/` → exit 0, `intact: true`.

| Severity | File / behavior | Evidence | Required correction |
| --- | --- | --- | --- |
| Medium (found and fixed inside the packet) | The summary's top-level `host` block was assigned per fixture inside the loop, so a multi-fixture run reported whichever host the **last** fixture used. A tape binds to one fixture id, so a second fixture legitimately falls back to `none` — and the summary would have described both runs with one block | new test "separates the host that was requested from the hosts the fixtures actually ran on"; mutation check 7 | Fixed: `host` now reports what was requested (`requested`, `eventsPath`, `kindsUsed`), and every run carries its own effective host with that tape's hash. Regression test and mutation check added. |
| Medium (design, accepted deliberately) | A tape is hand-authored data, so a `Passed` from a tape host says the harness works, not that the game works | `host.ts` watermark, `gateEligible: false`, mandatory `source`/`producedBy`, `note` on every summary | No code change. The mitigations are unconditional and the wording in the summary, the log and the bundle all say it. W0-07 supplies the first non-human `producedBy`. |
| Low | `identity.build.sourceDigest` differs between `dist` and the TypeScript sources under vitest, so the two are not comparable | `identity.ts`, `sourceKind` field | Recorded in the field itself rather than hidden. A packaged build always reports `dist`. |
| Low | The bundle copies the fixture and the tape in full, so a large tape is stored twice (evidence copy plus bundle copy) | `runner.ts` `writeArtifacts` + `withBundle` | Acceptable at fixture sizes (2 MiB tool limit). Revisit if P3 seed batches make bundles heavy. |
| Low | `run.log` is bounded, but the run **summary** is not — a fixture with 1,000 assertions produces a large JSON on stdout | `FIXTURE_LIMITS.assertions` caps it at 1,000 | Left as is: the summary is the machine contract and truncating it would be worse than a big file. Watch it at P3. |
| Info | `--evidence` defaults to `clanlab-out/`, which is created in the working directory | `.gitignore` now ignores it; `--help` documents it | Intentional: a failing run must leave a bundle behind even when the operator forgot the flag. |
| Info | Contract v0 cannot express a rejection reason, so `LAW-NOTICE-REJECTION`'s first assertion is Blocked, not Failed | `runner.ts` `judgeAssertion`; mutation check 2 | Not fixable here. Raised as an open question in STATUS and the handoff: which packet freezes the rejection event payload, and does `CommandAck` enter the committed-event stream as a typed event? |

Contract, ownership, hidden-state and serialization findings: contract v0 is untouched (`identity.contract.recordsDigest` covers all 16 records and no schema changed). The lab keeps its boundary — no `three`, `react`, DOM or `apps/web` imports, no `Math.random`; Node built-ins and the clock are used only in the CLI layer, and the clock is injectable so the reproducibility test can pin it. The tape adapter reuses the frozen `CommittedEvent` validator rather than declaring a second event shape, which is the first time a lab tool has consumed the W0-04 contract in anger — mutation check 6 proves the validation is load-bearing.

Acceptance criteria: 1 **PASS**, 2 **PASS**, 3 **PASS** — each demonstrated through the real CLI in `evidence/cli-transcript.txt` and re-run after the Medium fix, with the bundle it produced committed under `evidence/bundle-sample/`.

Verdict: **ACCEPTABLE_FOR_INTEGRATION** — one Medium found and fixed in-packet with a regression test, one Medium accepted as a deliberate design limit with unconditional mitigations, three Low, two Info.

This is review of the returned candidate, not proof that later merged code passes. The packet stays READY_FOR_REVIEW until Jani accepts it in chat; nothing here self-marks ACCEPTED.
