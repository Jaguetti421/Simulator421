# Independent review — W0-07 / attempt 1

Reviewer / role / fresh-context declaration: same developer, **same session as the author** — not a fresh-context review. The relaxed one-window rule applies; Jani accepts in chat. W0-05's review was genuinely fresh-context; this one is not, and that difference stays visible in the record.
Author / base / commit: same developer; base `f587b88` (W0-06 ACCEPTED) → `dc0f944` (pushed red, see below) → `bde23c4` → this commit.
Checks independently reproduced: `npm run verify` → 0 (build, lint, **383/383** vitest across 21 files, 6/6 python, workboard PASS); digests re-derived in three separate processes and a `worker_threads` worker; `clanlab run` on PERF-OPS-BASE against a kernel tape → exit 0, 13/13 passed; `node tools/gen_perf_ops_base.mjs` re-run → no diff; the branded-`Int` claim checked by compiling a probe file rather than asserted from memory.

| Severity | File / behavior | Evidence | Required correction |
| --- | --- | --- | --- |
| High (process, not code) | Commit `dc0f944` was pushed while lint was failing: the shell chain reported the failure but did not gate the push | `bde23c4` fixes it; the failure was a real architecture violation (`core` importing `host`) | The lint rule did its job; my command chain did not. Push must be gated on the verify exit code. Recorded in the handoff and in STATUS rather than quietly amended away. |
| Medium (scope, accepted) | The deferred typed lint rule for bare arithmetic on branded `Int` is **not** delivered | probe compile: `const x: Int = a + b` → TS2322; `a + b > 0` compiles | Partly unnecessary (the brand already protects everything flowing into `Int` state), partly still open (expressions consumed as plain `number`). Re-homed with the narrowed scope stated, not silently dropped. |
| Medium (design, deliberate) | Six of the ten stages have empty bodies | `STAGES` table, each with a name and a guarantee; `stage.NN.name` markers commit for all ten | Correct at W0-07: the transaction is the deliverable, not the systems. An empty stage that still runs and is still asserted is honest; a missing stage would not be. |
| Low | Synthetic per-tick timing is noisy on one CPU (P99 4.695 ms in run 1, 0.348 ms in run 3) | `evidence/timing.json`, three runs recorded rather than a best-of | Kept as three runs with the environment caveat in the same file. Budget evidence remains HUMAN_REQUIRED on reference hardware. |
| Low | `tests/kernel/**` is outside the card's listed in-scope paths | the card lists `packages/sim/core/**`, `packages/sim/host/**`, `tests/fixtures/PERF-OPS-BASE.json` | Unavoidable: cross-runtime equality needs `worker_threads` and the filesystem, which `packages/sim` may not import. Additive, and noted here and in the handoff. |
| Low | The kernel tape is not committed (196 KB) | regenerated by `tests/kernel/kernel.test.ts` and by the handoff's command | Acceptable because it is deterministic — the packet's own claim. If a future reviewer needs a frozen copy, the digest in `evidence/digests.txt` identifies it. |
| Info | Counters are excluded from the authoritative hash | test: same world, 5-event and 100,000-event log limits, identical digests | Matches CONVENTIONS.md: hashes exclude diagnostics. |

Contract, ownership, hidden-state and serialization findings: contract v0 untouched. `packages/sim` purity holds — no Node built-ins, DOM, timers, clock, `Math.random` or floats in the kernel; lint enforces it and caught the one violation I introduced. The stage-2 rejection path deliberately keeps `reasonId` out of the committed event, so W0-06's blocked-assertion finding stays true rather than being quietly bypassed.

Acceptance criteria: 1 **PASS** for the Node half and **BLOCKED_RENDER** for the browser half (recorded, naming W0-10, exactly as the card specifies), 2 **PASS**, 3 **PASS**.

Verdict: **ACCEPTABLE_FOR_INTEGRATION** — one High process finding (a red push, fixed and recorded), two Medium (one scope item re-homed with a narrowed claim, one deliberate design limit), three Low, one Info.

This is review of the returned candidate, not proof that later merged code passes. The packet stays READY_FOR_REVIEW until Jani accepts it in chat.
