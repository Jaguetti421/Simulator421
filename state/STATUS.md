# Project status (web build)

Owner: the developer. Updated 12 September 2026 (G0 approved, P1 begins; session 2).

- Kit: The_Last_Clan_Web_Kit_v1 (kit zip SHA-256 `11136c86390b4b146c6eb5de3f87125c25ffdabc18318f6c7e1204d7fa06f179`), imported frozen under `docs/production/` — 196/196 files verified against its MANIFEST.json; live copies at the repository root (see README.md "Layout").
- Baseline: the W0-01 bootstrap commit `ed2766fec321584f9f021a1b8c591ffcef9e2ad6` (see state/ACCEPTED_BASELINE.json). W0-01 ACCEPTED on top of it; ACCEPTED_BASELINE.json is re-cut at the G0 tag (W0-11).
- HEAD at session end: cannot be written here without changing itself (same circularity as the archive hash). The sidecar `lastclan-W0-01-a01.zip.sha256` lists both the archive SHA-256 and the HEAD commit; next session verifies `git rev-parse HEAD` against it and expects `git log --oneline` to show the two W0-01 commits on top of nothing.
- Accepted packets: 15 / 133 shipping + 6 optional PX (W0: 11/11, P1: 4/35). Gates passed: **G0** (approved 12 Sep on tag `w0-baseline`, two human checks deferred to G1).
- Current phase: W0. W0-01 ACCEPTED (producer acceptance, 11 Sep). **W0-02 ACCEPTED** (producer acceptance, 12 Sep; primitives in `packages/sim/primitives`, 94/94 tests). **W0-03 ACCEPTED** (producer acceptance, 12 Sep; sfc32 + labeled streams + two-domain hashing, 124/124 tests). **W0-04 ACCEPTED** (producer acceptance, 12 Sep; contracts v0 frozen — 16 records, 16 schemas, 25 golden samples, reason registry, intra-sim dependency direction in lint; 233/233 tests). **W0-05 ACCEPTED** (fixture DSL v1: envelope, semantics, assertion registry, `clanlab validate`; 303/303 tests. Jani accepted in chat 12 Sep; `handoffs/W0-05/REVIEW.md` is the project's first genuine **fresh-context** review — new session, new sandbox, criteria re-run rather than read: 3 Low, 0 material). **W0-06 ACCEPTED** (producer acceptance in chat, 12 Sep; `clanlab run` and `clanlab inspect`: declared hosts, machine summaries with build/contract/content/geometry identity, bounded logs, reproducible failure bundles; 350/350 tests. Same-session review: 1 Medium found and fixed in-packet, 1 Medium accepted as a design limit, 3 Low). **W0-07 ACCEPTED** (producer acceptance in chat, 12 Sep; ten-stage tick transaction, pause barrier, command queue, synthetic 136/137-actor workload, counters, per-tick timing, authoritative hashes equal across Node main thread and `worker_threads`; PERF-OPS-BASE runs green through `clanlab` on a kernel-produced tape; 383/383 tests). **W0-08 ACCEPTED** (producer acceptance in chat, 12 Sep; versioned section container, async storage contract with in-memory / fake-indexeddb / **real browser** IndexedDB adapters, two-generation durable saves, world snapshot codec with `SimHost.save()/restore()`, exactly-once results; 426/426 vitest plus 3 Playwright tests). All three criteria PASS — including the browser adapter, which the card expected to be BLOCKED here and which actually ran. **W0-09 ACCEPTED** (producer acceptance in chat, 12 Sep; deterministic 2D readability renderer, `clanlab render`, PNG metadata, TUNE readability assertions; 442/442 tests). All three criteria PASS. **W0-10 ACCEPTED** (producer acceptance in chat, 12 Sep; Vite static site; the W0-07 kernel in a Worker at 1x/2x/4x with pause and a confirmed-tick readout; Three.js tabletop scene with the GDD 14.2 camera; the eight Prototype8 procedural identities with recorded bounds and attachment points; `/capture` with warm-up frames, PNG metadata and an in-image FakeSim watermark; the CI screens job enabled. 461/461 vitest + 16 Playwright). Criteria 1, 3 and 4 PASS; criterion 2 passes structurally and its visual half is **HUMAN_REQUIRED** — `handoffs/W0-10/evidence/identity-kit-prototype8.png` is the artefact for that review. **W0-11 ACCEPTED** and **G0 APPROVED** by the producer in chat on 12 September 2026 ("Accepted and continue. I'll have to postpone testing and do it later."), on tag `w0-baseline`. All six G0 criteria executed and passed. **Two human checks were deferred, not satisfied** — the visual judgement of the eight procedural identities and GPU frame timing on reference hardware — and both are recorded in `state/gates.json` under `deferredHumanChecks`, carried to **G1**. Jani was explicit about continuing without feedback, so **W0 is closed and P1 has begun**. **P1-01 ACCEPTED** (producer acceptance in chat, 12 Sep) — the action lifecycle contracts are complete. **FIX-08 and FIX-09 (14 Sep): the `Int` debt is cleared — all 53 sites.** Every file that was allow-listed is now routed through `checkedMath`: visibility 20, terrain 19, route 4, tick 3, island 2, host 1. Nothing in `packages/sim` is allow-listed any more except the arithmetic primitives themselves and the tests, and the four-packet deferral is closed rather than counted.

**The map moved, and that is the honest part.** The old code used `Math.trunc` (toward zero) where `checkedMath` divides toward negative infinity, so the coastline interpolation disagreed by one wherever it went negative. The geometry hash changed twice as the rewrite landed (`45dd33b6` → `70bc28b8` → `178ade0a`) and the island still validates against every R1 rule. I took floor rather than preserving trunc: floor is the house convention for every other consequential value, no hash was pinned as a golden, and keeping two rounding rules in one codebase to protect a hash nobody depends on would be the worse trade. It is recorded in the code at the site, not just here.

**FIX-07 (14 Sep): the typed `Int` rule is wired and enforced.** The reviewing agent's `no-bare-int-arithmetic` is compiled by `npm run build` (a new `tools/eslint` project), imported by `eslint.config.js`, and run with type-aware parsing over `packages/sim`. If the compiled rule is missing the config **throws** rather than linting without it: a silently skipped guard is how a rule stops guarding unnoticed. A live test runs ESLint on a probe file and requires the rule to reject `(a + b) > 0` on branded `Int`s.

It found **53 real violations** on first run — exactly the gap I had measured four times and never closed. `spatial/visibility.ts` is **cleaned** (20 sites now routed through `checkedMath`; two height accessors now return `Int`; the bucket-count constant is deliberately a plain `number` because it is an array shape, not a world quantity). At the time of FIX-07 the remaining 29 sites were allow-listed by file with per-file counts, so the debt would shrink visibly rather than be forgotten. FIX-08 and FIX-09 cleared all of them the same day.

**Second red push, same cause as the first (`f9bf110`, 14 Sep).** I put the rule's live test inside `packages/sim`, which has no Node types by design, so `tsc` failed — and the commit went out anyway because the shell chain used `;` between verify and push instead of `&&`. This is exactly the failure I wrote a rule about after `dc0f944` in W0-07 and then repeated. Fixed in the next commit (the test moved to `tests/arch/`, where shelling out to ESLint is allowed); recorded here rather than amended away. **The habit to enforce mechanically, not remember: verify and push must be one `&&` chain.**

One wiring note: the architecture probes lint synthetic in-memory files, which type-aware parsing cannot resolve, so those paths are excluded from the typed block — they test the untyped boundary rules, not this one.

**FIX-06 done (14 Sep): the G1 scene (R3) — the design rulings are now fully applied.** The scene is a declared **180 × 180 window into the shipping island**, not a second grid: a separate grid would have meant making every spatial query grid-parametric and giving the scene a different geometry hash from the island it is a corner of. Its sockets are **derived from the compiled world** — a start is the nearest walkable cell to its intended spot — so a map change moves them instead of leaving them in the sea; a test compiles seed 4108 and requires all eight to land on ground there too. Every R3 rule passes, measured: 83.7 % land, sea on an edge, two authored crossings inside the window, one obstacle cluster, eight starts, slowest walk to food **18 s** (limit 45) and to camp **33 s** (limit 90), by real routes at 3.5 m/s. Two crossings were added inside the window, and authored crossings are now **clamped below the waterline**: height noise had lifted the one at y=640 into dry ground — walkable, but not the wadeable ford R2 asks for, and the kind of difference that shows up on one seed only. Geometry hash `45dd33b6`.

**FIX-05 done (14 Sep): the shipping map is under test, and D05 is measured (R1, R4).** `walkingSecondsBetween` and `routeMilliseconds` compute walking time at 3.5 m/s with the shallow-water multiplier paid where the route actually crosses it; `validateAnchorTimes` turns that into the acceptance R1 asks for. Measured on the island: **N–S 196 s, W–E 356 s, N–E 201 s, W–S 199 s** — every pair routable, and the longest crossing inside D05's 240–360 s band. The shortest is reported as **informational, not a failure**: a valley is fast along its floor and slow across its stream, so 196 s N–S is the shape working rather than a defect. A full-island route costs ~130–1,900 ms, so this is a real check rather than a sampled one. Six new tests run sight and routes on the shipping `valley` — open floor at the sight limit, across the stream at a crossing, out to sea from the shore, blocked by a rim escarpment away from its passes, the D05 pairs, crossing the stream without swimming, and refusing to route into the sea.

**FIX-04 done (14 Sep): the shipping island (DESIGN-RULINGS-01 R1–R2).** The old `valley` is renamed **`trough`** and kept as a test template — it is the only one that instances every traversal class, which is a test property, not a design one. The shipping **`valley`** is now an island: sea at every envelope edge, a 12-cell shallow coastal band on a wandering coastline, land interior, a stream with three authored wadeable crossings and deep pools capped at 26 cells, and rim escarpments with two passes. The manifest carries an **R1 validation measured from the compiled cells** — land 58.0 %, shallow 10.7 %, sea at the edge, largest inland water body 2,801 cells — all four rules PASS. Geometry hash `7e9aa06e`.

**Finding for the reviewer, recorded rather than tuned around:** R1 asks for 8–12 % shallow water *and* a 3–12 cell coastal band, and those two numbers do not meet. A 12-cell band around a single island of this size yields about **4 %** however convoluted the coastline is made — I swept five coastline parameter sets to check. The remaining shallow is an authored **lagoon** on the south-west shore. If the band alone was meant to reach 8 %, one of the two numbers needs to move, and that is the reviewer's call, not mine.

Two lint catches worth noting: the R1 fraction bands were float literals (now thousandths), and the island mask used `Math.sqrt`/`atan2`/`PI` (now `isqrt` and an integer pseudo-angle). Terrain is consequential and the float ban applies to it; the rule caught both.

Still open from the rulings: **R3** (the 180×180 G1 scene), **R4** (shipping map under the sight and route suites), and the D05 anchor route-time check in the validator.

**FIX-02 and FIX-03 done (14 Sep).** **FIX-02 (P1-01 contracts):** two refinements could never fail — one ended in `length >= 0`, one returned `true` — and a third duplicated a schema bound, so it was unreachable rather than merely weak; all three are replaced or removed. Added **`ActionInstance`**, the in-flight action a snapshot must carry (24 records; digest `sha256:2fbef43a…`), with the interruption rule that an interrupted action holds no reservations. Every refinement on the eight P1-01 records now has a **crafted reject sample** and a liveness test that fails if any refinement accepts all of them; extending that to the sixteen W0-04 records needs a sample per refinement and is named, not skipped. The "completing TP §8" claim is corrected: the records **declare** the remaining categories, preconditions are still implied by `dependencies`, and nothing executes any of them yet. **FIX-03 (P1-02 geometry hash):** the XOR-of-five-hashes is replaced by one running hash over a canonical byte stream — header, then each array in fixed order, length-prefixed, explicitly little-endian. A test constructs the array swap the old scheme accepted and requires the new one to reject it. The hash changed (`4567e8a7` at seed 4107); no geometry hash had been stored as a golden, which is why this was worth doing now.

**REVIEW-EXTERNAL-01 received (14 Sep)** — twelve fresh-context reviews from the reviewing agent, plus `DESIGN-RULINGS-01.md` (binding) and a delivered ESLint rule. Verdicts: W0-10 and W0-11 **CHANGES_REQUESTED (High)**, P1-01 and P1-02 CHANGES_REQUESTED (Medium), the rest ACCEPTABLE_FOR_INTEGRATION.

**FIX-01 done (14 Sep): the High finding.** The capture camera targeted `[0,0,0]` while every actor stood near 400 m, so **both PNGs cited as G0's 3D evidence were empty** — and I had described them to Jani as showing characters without looking at them. Corrected: the camera targets the actor nearest the crowd centre; the capture tests now assert actors project inside the viewport **and** that the middle of the frame is not background (decoded pixel check); both images regenerated and **looked at**; `GATE_EVIDENCE.md`'s screenshot row rewritten to record the original error and the correction. A second finding fell out of the fix: at the GDD's tabletop band (220–420 m) a 1.8 m actor is about four pixels tall, so the 3D camera cannot serve as an island overview — that is the 2D readability renderer's job, and the evidence captures are now taken at 22–30 m where a person can see people.

Still to do from the review, in Jani's order: contracts patch packet (dead refinements, refinement-liveness test, `ActionInstance` state section, reword "completing §8"); canonical geometry hash; DESIGN-RULINGS-01 (rename the current valley to `trough`, author the shipping `valley` and the 180×180 G1 scene, put the shipping map under the sight and route suites); then the remaining ranked Mediums. **DEBT-02 READY_FOR_REVIEW** — **fixture DSL v2**: the `ToolCheck` assertion kind closes the gap five registry fixtures hit across four packets. A fixture can now state a readability, boundary or progress claim; `clanlab render` performs the three readability checks, `clanlab run` reports each as Blocked and names its provider, and v1 fixtures still parse. The supplied schema carries the same addendum, so the conformance guard still compares the parser against a schema rather than against itself. 570/570 tests. **P1-04 ACCEPTED** (producer, 14 Sep) — knowledge-limited route segments: an integer A* over a per-actor knowledge overlay, with Complete / Partial / BudgetExhausted / NoKnownRoute kept distinct and bounded recovery when a certified portal stops making progress. Walling off 40 unobserved cells leaves a route byte-identical, and a companion test proves that is not vacuous. 567/567 tests. **`ROUTE-PROGRESS` is the fifth registry fixture that fixture DSL v1 cannot express** — that gap now needs a scheduled packet. **DEBT-01 ACCEPTED** (producer, 12 Sep: "good job") — a producer-directed pass (12 Sep: "fix everything you can in the past") closing gaps earlier packets deferred: `clanlab run --host kernel` runs the real W0-07 kernel and submits the fixture's schedule, so **reason matching now resolves against acknowledgements** (`LAW-NOTICE-REJECTION`'s assertion, Blocked since W0-06, passes), **SaveReload is evaluated** rather than blocked, every stale `availableFrom` naming W0-07/W0-08 is gone, and an assertion about a system that does not exist is **Blocked rather than Failed**. 554/554 tests. **Closed 12 Sep by DEBT-02** (fixture DSL v2). The typed `Int` lint rule remains undelivered. **P1-03 ACCEPTED** (producer acceptance in chat, 12 Sep) — the broad-phase spatial hash and height-field line of sight: symmetric by construction, with an explicit budget that is never `visible`, and a sight query that takes terrain and two positions and has no parameter an actor's knowledge could arrive through. 542/542 tests. TP §6's perception model — field of view, hearing, light, sleep, belief fields, the 0.5 s threat bound — is **not** in it. **P1-02 ACCEPTED** (producer acceptance in chat, 12 Sep) — the terrain and fine geometry compiler: an 800 × 800 one-metre integer height field (640,000 cells), traversal classes from compiled height and slope, authoritative obstacle footprints, sparse 0.25 m detail around sockets, and **one geometry hash** that the render export and the simulation manifest both report (`b0a3a4eb` at seed 4107). 524/524 tests. TP §5's later stages — walkability validation, resources, spawn neighbourhoods, knowledge distribution, phase-escape and seed rejection — are **not** in this packet and nothing here implies a playable seed. **The contract surface moved from 16 to 23 records** and its digest from `sha256:291f9b94…` to `sha256:3fe2ced1…`: five records completing TP v1.1 §8 (resources, execution, interruption, completion, failure), `StateSectionSet`, and **`AppearanceRecipe`, frozen at Jani's direction**, closing the W0-10 finding. 14 new golden samples, 509/509 vitest + 17 Playwright. No existing record was edited, so old artifacts remain truthful records of the surface they were made against.
- Continuity: **GitHub is now canonical.** `https://github.com/Jaguetti421/Simulator421` (private), branch `main`, pushed 11 Sep 2026 after Jani granted the token Contents: Read and write (first attempt was refused 403 read-only). Token used only via environment variable + per-command header; never in files. Each session: clone/fetch with the session token, `npm ci && npm run verify` on the baseline, push at every green step. Archives (`lastclan-<packet>-a<NN>.zip`) are the fallback only when no token is given.

## Environment observed in this sandbox (W0-01, 11 Sep 2026 — supersedes the AGENTS.md note)

| Item | Observed |
| --- | --- |
| OS / hardware | Ubuntu 24.04.4 LTS, x86_64, 1 CPU, 3.9 GiB RAM |
| Node / npm / git / Python | Node v22.22.2, npm 10.9.7, git 2.43.0, Python 3.12.3 (PyYAML available) |
| npm registry | reachable (all installs below succeeded) |
| github.com | reachable (`git ls-remote https://github.com/git/git.git` succeeds); push not tested — no token this session |
| api.github.com | reachable (HTTP 403 = unauthenticated rate limit; other hosts return the proxy's `x-deny-reason: host_not_allowed`) |
| Browsers | **present**, contrary to the kit note: `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers` with `chromium-1194` (Chromium 141.0.7390.37), `chromium_headless_shell-1194`, `ffmpeg-1011`; also `/opt/google/chrome`. Build 1194 = **playwright-core 1.56.x** (read from its browsers.json; 1.57 wants build 1200, which cannot be downloaded here — cdn.playwright.dev is not an allowed host) |
| WebGL2 in headless Chromium | **works via SwiftShader** (renderer "ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero)))", WebGL 2.0 (OpenGL ES 3.0 Chromium)); `navigator.gpu` present. Software rasterizer: usable for 3D captures and correctness, **not** for performance or GPU-quality evidence |
| GPU | none (`/dev/dri` absent, no `nvidia-smi`); Xvfb present but not needed for headless |
| node-canvas | canvas 3.2.3 installs from prebuilt and works (2D draw + PNG encode) |
| headless-gl | gl 8.1.6 installs but `require('gl')(16,16)` returns null |
| CI runner | none exercised; ci.yml validated locally (PyYAML parse + actionlint v1.7.7, no findings). CI execution is BLOCKED_TOOL until a push happens |

## Toolchain pins (locked at W0-01; change only with a note here)

| Package | Pinned | Reason |
| --- | --- | --- |
| typescript | 6.0.3 | 7.0.2 is the registry `latest`, but typescript-eslint 8.70.0 supports `>=4.8.4 <6.1.0`; 6.0.3 is the newest supported stable |
| eslint / @eslint/js | 10.10.0 / 10.0.1 | flat config (`eslint.config.js`); ESLint 10 has no `.eslintrc`, so the card's `.eslintrc*` path is realised as `eslint.config.js` |
| typescript-eslint | 8.70.0 | recommended rules + `no-restricted-types` for DOM type bans |
| vitest | 5.0.0 | vite 8.3.0 arrives as its peer; Vite itself is adopted for apps/web at W0-10 |
| globals / @types/node | 17.12.0 / 22.20.2 | Node 22 globals and types for lab, content, tests |
| fast-check | 4.10.0 | property tests (W0-02) |
| @thi.ng/random | 4.1.54 | dev-only: independent SFC32 to cross-check the PRNG step (W0-03) |
| ajv | 8.20.0 | dev-only: independent JSON Schema (draft 2020-12) validation of the golden samples (W0-04) |
| Planned at later packets | canvas 3.2.3 (W0-09), @playwright/test 1.56.x (W0-10, must match installed browsers), three, react 19.x, fake-indexeddb 6.x (W0-08) | pinned when introduced, with a note here |

## Decisions recorded at W0-01

1. **Layout:** live kit directories at the repository root (`state/ work/ tools/ templates/ handoffs/ contracts/ design/ agents/ reference/`, plans, AGENTS/CLAUDE/CONVENTIONS) because every card, tool and instruction addresses them root-relative; `docs/production/` holds the delivered kit frozen and hash-verified (`tools/test_tools.py::KitSnapshot`). Only `adr/` and `KIT_IMPORT.md` may be added there.
2. **No `src/` inside packages:** modules live directly under the package directory (`packages/sim/primitives`, `packages/lab/cli`, …) exactly as CONVENTIONS.md maps them; tests are colocated `*.test.ts`. `apps/web` keeps `src/`.
3. **`packages/sim` is pure by type system and lint:** tsconfig `lib: ["ES2023"]`, `types: []`; ESLint bans render/app imports, Node built-ins, DOM/timer/clock globals and types, `Math.random`, transcendental `Math.*`, `parseFloat` and float literals in production files; sim tests may use Node and vitest but keep the render/DOM ban. W0-04 adds the intra-sim direction (`ai` ↛ `core` implementation).
4. **clanlab stub exits 3 (`NOT_IMPLEMENTED`)** for `validate|run|render`; the CI fixture step is `continue-on-error: true` with that reason written next to it, to be removed at W0-06. No stub output can be mistaken for a PASS.
5. **Archive hashing:** an archive cannot contain its own SHA-256. The HEAD commit is recorded here; the archive's SHA-256 travels in a sidecar `lastclan-<packet>-a<NN>.zip.sha256` and in chat. Next session verifies both.
6. **Playwright plan (for W0-10):** pin @playwright/test 1.56.x to the installed browsers; capture 3D screenshots in the sandbox with SwiftShader and label them software-rendered; keep GPU performance/quality as HUMAN_REQUIRED on Jani's PC.

7. **W0-02:** checkedMath asserts safe integers always (not only in test builds); `mulDiv` slow path is pure integer long multiplication with stated preconditions; the typed lint rule against bare arithmetic on `Int` is deferred to W0-04.

8. **W0-03:** the sfc32 *step* is the published algorithm and is cross-checked against an independent implementation; the *seeding rule* is project-defined, versioned by `RANDOM_SEEDING_RULE` and pinned in `packages/sim/primitives/vectors/random-v1.json` (regenerate with `node tools/gen_random_vectors.mjs`). Hash order-independence is achieved by canonical key ordering, not a commutative combiner. Sim tests load the vectors as a typed JSON module, so `packages/sim` still compiles with no Node types anywhere.

9. **W0-04:** contract records are declared once in a small typed DSL that emits the TS type, the validator, the JSON Schema and the canonical codec, so those four cannot drift; cross-field invariants live in `refine(...)` and are annotated as `x-refinements` in the emitted schema (JSON Schema cannot express them, and the sample manifest records which layer enforces each rejection). The intra-sim dependency direction from INTERFACES.md is now lint, composed into **one** `no-restricted-imports` rule per module — a second block for the same rule key would have silently replaced the first. Operation and event payload *fields* stay open at v0 and are frozen per operation by the packet that implements it.

10. **W0-05:** the fixture envelope is re-declared with the contract DSL and kept honest by a conformance test that runs every case through both ajv (the supplied `contracts/fixture.schema.json`) and the parser, requiring agreement on structural verdicts and requiring semantic-only rules to be genuinely beyond JSON Schema. Two of my own claims were corrected by that discipline: an invented "law must start before maxTicks" rule was deleted after it rejected a supplied example, and the tick-0 schedule rule turned out to be structural. Skipped checks (no content catalog) and Blocked assertions (no simulation) are reported as such and can never read as passes; `clanlab validate` is implemented and CI no longer needs `continue-on-error` on the fixture step.

11. **W0-06:** `clanlab run` is the harness, not a simulation. Events come only from a **declared** host — `none` (no run: every assertion Blocked, exit 4) or `tape` (`--events`, every event validated against the frozen `CommittedEvent` contract, and the file must state `source` and `producedBy`). A fixture's own `expectAck` is never turned into an event: deriving the outcome from the expectation would confirm it with itself. Both hosts are watermarked FakeSim with `gateEligible: false`. **Blocked is nonzero** (exit 4; FIXTURE_DSL.md says nonzero means failed, blocked *or* invalid), and 1 takes precedence over 4. Failure bundles carry copies of every input plus a `verdictDigest` that excludes the clock and the operator's paths, so the same failure hashes the same twice and `clanlab inspect` recomputes it rather than trusting it.

12. **W0-07:** the stage ordinal in a committed event is the position the stage **executed** at, so PERF-OPS-BASE asserts stage *order* and not merely stage presence. The rejection reason stays out of the committed event (contract v0 carries none) and is available only from `SimHost.outcomes()` — encoding it in the event type would have bypassed the W0-06 finding. Diagnostics (counters, draw counts, the bounded event log) are excluded from the authoritative hash. `PERF-OPS-BASE.json` is generated from the kernel's own initial world by `tools/gen_perf_ops_base.mjs` and re-derived by a test, so fixture and workload cannot drift. **The deferred typed lint rule for bare `Int` arithmetic is still not delivered**: measured, `const x: Int = a + b` already fails to compile, so the open gap is only expressions consumed as a plain `number` (comparisons, indices, `number` parameters) — re-homed to the first P1 packet with comparison-heavy consequential arithmetic.

13. **W0-09:** the readability renderer draws nothing that does not exist. Terrain is one declared `unavailable` class (no map compiler), the ring encodes actor kind (no clans until P2) and action icons are assigned from actor id purely to exercise the set (no actions yet) — all three are listed as skipped checks in every render summary. The twelve icon names are **provisional**; what is verified is that they rasterize distinctly. Readability thresholds are **TUNE** from one synthetic scene plus WCAG 1.4.11, and do not stand in for PRESENT 01's human half. Fixture DSL v1 cannot express a readability assertion, so the verdict lives in the render summary and the exit code, and adding that assertion kind is a named gap for a later lab packet.

15. **P1-01:** the action lifecycle is completed by **new records, never edits to the existing sixteen** — so the contract digest changes once, explicitly (`291f9b94…` → `3fe2ced1…`), and no packet that already depends on those shapes needs re-reviewing for a silent field. `AppearanceRecipe` is frozen (Jani's direction, 12 Sep), carrying palette **indices** so colour stays app-side, with a test binding the app kit's slot families to the contract enums. Failure records carry an `intentTemplateId`, never display text (TP §12). The contract consumer map is **name-based on purpose**: a person making a contract change greps for the name, so a record covered only by a generic loop is exactly the one that gets missed — it found three on its first run.

14. **W0-10:** `AppearanceRecipe` is cited by TP v2.0 as an existing contract but is **absent from contract v0's sixteen records**. It lives app-level and versioned in `apps/web/src/kit/recipe.ts` rather than being added as a seventeenth record from an app packet, which would change the contract digest in every summary this build emits — freezing it properly is proposed work for a contracts packet. The scene uses one `InstancedMesh` per primitive **shape** rather than per kit part (documented deviation). `/capture` reads the kernel's own W0-08 snapshot, so image and state cannot diverge. Software WebGL proves layout and correctness only: a 1280x720 capture exceeded a 30 s timeout where 960x540 takes under a second, which is why frame budgets stay HUMAN_REQUIRED.

## W0 phase metrics (G0 candidate)

| Metric | Value |
| --- | --- |
| Packets | 11, all shipped; 10 ACCEPTED, W0-11 awaiting acceptance |
| Sessions | 3 (W0-01…W0-04 and W0-05 authoring in sessions 0–1; W0-05 review through W0-11 in session 2) |
| Attempts per packet | 1 each — no packet needed a second attempt |
| Tests | 303 at the session-2 baseline → **461 vitest across 28 files**, plus **17 Playwright** browser tests |
| Fixtures | 3 supplied examples validated; 3 authored (PERF-OPS-BASE, SAVE-ROUNDTRIP, PRESENT-READ-01), all passing against kernel-produced tapes |
| Rework found by review | 1 Medium fixed in-packet in W0-06 (requested vs effective host), 1 Medium in W0-08 (sync storage interface, fixed before the adapters), 1 Medium in W0-09 (PNG tEXt encoding), 1 Medium in W0-10 (my own instance-count assertion) |
| Blocked checks cleared | CI execution (BLOCKED_TOOL, W0-01 → cleared W0-05); browser determinism (BLOCKED_RENDER, W0-07 → cleared W0-10); browser IndexedDB and `/capture`, both of which cards expected to be BLOCKED in the sandbox and both of which ran |
| Blocked checks remaining | 0 automated. 2 HUMAN_REQUIRED **deferred by the producer to G1**: visual judgement of the eight identities, and GPU frame timing |
| Time to first runnable slice | W0-01 produced a running `clanlab` stub; the first slice that *did* something real was W0-05's fixture validation, and the first slice a person could watch was W0-10's shell |
| Process failures | 1 red push (`dc0f944`, lint failing, pushed because a shell chain did not gate on the exit code; fixed in `bde23c4`) |
| Reviews | 11 written; **1 genuinely fresh-context** (W0-05). The rest were same-session under the producer-approved relaxation, each declaring it — the largest caveat on G0 |

## Session log (one line per session)
| date | packet | session # for packet | status at end | tests added | fixtures authored/failing/passing | blocked checks | rework after self-review | human needed |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-11 | W0-01 | 1 | ACCEPTED (producer) | 49 vitest (44 boundary cases, 5 clanlab) + 2 python (6 total in tools) | 0 / 0 / 0 | 2 (CI runner execution: BLOCKED_TOOL; GitHub push: refused 403, token lacks write) | waived — fresh-context review replaced by Jani's explicit acceptance; same-session review found 3 Low, 0 material | yes — token permission |
| 2026-09-12 | W0-02 | 1 | ACCEPTED (producer) | 44 vitest (13 fast-check properties) | 0 / 0 / 0 | 0 | waived — producer acceptance; same-session review found 2 Low | no |
| 2026-09-12 | W0-03 | 1 | ACCEPTED (producer) | 30 vitest (13 fast-check properties) + 4 mutation checks | 0 / 0 / 0 | 1 (cross-runtime stream equality: deferred to W0-07/W0-10 by the card) | waived — producer acceptance; same-session review found 2 Low | no |
| 2026-09-12 | W0-04 | 1 | ACCEPTED (producer) | 109 vitest (44 contracts, 29 samples/ajv, 36 arch) + 3 mutation checks | 0 / 0 / 0 | 0 | waived — producer acceptance; same-session review found 1 Medium (found and fixed in-packet, with a regression test) and 3 Low | no |
| 2026-09-12 | W0-05 | 1 | ACCEPTED (producer + fresh-context review) | 75 vitest (53 lab, 22 conformance) + 4 mutation checks | 3 authored / 0 failing / 0 passing (parse-only; running them is W0-06) | 2 (Invariant assertions → W0-07; HashEqualVariant → W0-08) | none material — fresh-context review (session 2) reproduced all three criteria and found 3 Low | no |
| 2026-09-12 | W0-06 | 1 | ACCEPTED (producer) | 47 vitest (16 runner, 13 host, 12 bundle/inspect, 4 log, 2 CLI) + 7 mutation checks | 2 authored (1 deliberate-failure demo, 1 passing variant) / 1 failing as designed / 1 passing | 6 blocked assertions on the three supplied examples (no simulation until W0-07); reasonId matching blocked by contract v0 | 1 Medium found by the same-session review and fixed in-packet with a regression test (requested vs effective host) | no |
| 2026-09-12 | W0-07 | 1 | ACCEPTED (producer) | 33 vitest (18 core, 8 host, 7 cross-runtime/fixture) | 1 authored (PERF-OPS-BASE, 137 actors, 13 assertions) / 0 failing / 1 passing via a kernel tape | 1 (browser Worker digest: BLOCKED_RENDER until W0-10) | same-session review: 1 High process finding (red push, fixed in bde23c4), 2 Medium, 3 Low — no code rework required | yes — reference-hardware timing stays HUMAN_REQUIRED |
| 2026-09-12 | W0-08 | 1 | ACCEPTED (producer) | 43 vitest (17 container/generations/results, 16 shared adapter contract, 8 snapshot, 2 SAVE-ROUNDTRIP) + 3 Playwright | 1 authored (SAVE-ROUNDTRIP, 137 actors, 4 assertions) / 0 failing / 1 passing via a restored-run tape | 0 — the browser check the card expected to be BLOCKED executed and passed | 1 Medium found mid-packet and fixed before the adapters were written (sync storage interface) | no |
| 2026-09-12 | W0-09 | 1 | ACCEPTED (producer) | 16 vitest (purity, PNG metadata, readability metrics with both negative cases, CLI) | 1 authored (PRESENT-READ-01, 137 actors) / 0 failing / 1 rendered green | 0 — the renderer runs without a GPU, as TP v2.0 §13 intends | 1 Medium found by reading the evidence (PNG tEXt is Latin-1; metadata was being mangled) and fixed in-packet | no — human readability review (PRESENT 01, human half) is still owed |
| 2026-09-12 | W0-10 | 1 | ACCEPTED (producer) | 19 vitest (pacing, identity kit, camera) + 13 Playwright (shell, hash parity, capture) | 0 authored / 0 failing / 0 passing (the capture spec drives the route directly) | 0 — every browser check ran, including the one the card expected to be BLOCKED_RENDER | 1 Medium arithmetic error in my own instance-count assertion, caught by the test and corrected | yes — visual distinguishability of the eight identities, and GPU timing and 3D quality on Jani's PC |
| 2026-09-12 | W0-11 | 1 | ACCEPTED (producer); G0 APPROVED with 2 human checks deferred to G1 | 1 Playwright (offline boot with all external origins blocked) | 0 authored / 0 failing / 6 validated and replayed | 2 HUMAN_REQUIRED (visual judgement, GPU timing) — named in JANI_PC_CHECKLIST.md | none | yes — the gate stops for Jani |
| 2026-09-12 | P1-01 | 1 | ACCEPTED (producer) | 48 vitest (7 provider composition, 7 consumer map, 19 lifecycle samples, 15 regenerated artifact checks) | 14 authored samples (7 accept, 7 reject) / 7 rejecting as designed / 7 accepted | 0 | 3 records found with no named consumer test and covered; 2 of my own scripted edits silently did nothing before I caught them | no |
| 2026-09-12 | P1-02 | 1 | ACCEPTED (producer) | 15 vitest (hash identity, GDD speeds at real compiled cells, socket queries, fine detail) | 0 authored / 0 failing / 0 passing | 0 | 1 Medium found in-packet: the first compile produced zero Cliff cells — a class with no instances — fixed by authoring the valley rim | no |
| 2026-09-12 | P1-03 | 1 | ACCEPTED (producer) | 18 vitest (broad phase, occlusion, boundaries, budget, info isolation) | 0 authored — INFO-ISOLATION cannot be expressed in fixture DSL v1 (third packet to hit that gap) | 0 | 2 Medium found in-packet: sight was asymmetric; and my occlusion test was green against a world that cannot occlude | no |
| 2026-09-12 | DEBT-01 | 1 | ACCEPTED (producer-directed, not a workboard packet) | 11 vitest (kernel host, ack matching, SaveReload, unemittable types) + 3 earlier tests corrected | 0 authored / 0 failing / 6 fixtures now runnable against a real host | 0 new; 4 long-standing blocks cleared | n/a | no |
| 2026-09-12 | P1-04 | 1 | ACCEPTED (producer) | 13 vitest (knowledge isolation and its non-vacuity, four distinct outcomes, portal advance and bounded recovery) | 0 authored — ROUTE-PROGRESS cannot be expressed in fixture DSL v1 (fifth packet) | 0 | none | no |
| 2026-09-12 | DEBT-02 | 1 | READY_FOR_REVIEW (producer-directed) | 3 vitest (v2 parses, tool checks performed, a false claim fails) + 5 tests corrected | PRESENT-READ-01 upgraded to v2 and now states its own claims | 3 registered checks still have no CLI provider | n/a | no |
| 2026-09-14 | FIX-01 (REVIEW-EXTERNAL-01 W0-10 High) | 1 | DONE | 2 Playwright assertions added (actors in viewport; decoded central non-background fraction) | both G0 evidence captures regenerated and inspected | 0 | the High finding itself: empty images cited as gate evidence, never looked at | no |

## Risks and open questions
- **Toolchain versions post-date the developer's training data** (TypeScript 6, ESLint 10, Vitest 5, Vite 8, Playwright 1.56 browsers). Everything used at W0-01 was executed and observed; nothing is assumed. Expect occasional API surprises in later packets — verify by running, not by memory.
- **1 CPU / 3.9 GiB sandbox:** Vitest runs files serially (`fileParallelism: false`). The 137-actor workload (W0-07) and 100-seed batches (P3) will be slow here; wall-clock numbers from this sandbox are diagnostics only, never budget evidence.
- **Software WebGL only:** 3D captures in the sandbox prove correctness and layout, not frame budgets. Reference-hardware evidence is Jani's PC (templates/JANI_PC_CHECKLIST.md).
- **Token hygiene:** the token was pasted in chat; Jani may rotate it at any time — the developer needs it only per session. CI on the GitHub runner is **green**: the Actions API reports `completed / success` for `1a3d478`, `678fe94` and `e14e0a6` (checked 12 Sep, session 2), so the runner really executes build, lint, vitest, python and the workboard check. This clears the W0-01 BLOCKED_TOOL on CI execution.
- **W0-01 acceptance was producer acceptance, not a fresh-context review.** Recorded honestly in the metrics; later packets return to the fresh-context rule unless Jani says otherwise.
- **Process fix owed (W0-07):** commit `dc0f944` was pushed while lint was failing because the shell chain did not gate the push on the verify exit code. Fixed in `bde23c4`; the habit to keep is verify-then-push as one gated step.
- **Resolved (12 Sep, session 2) — the W0-06 "reasonId" question needed no decision from Jani; the repository's own authorities already answer it.** The reason does not belong on a committed event and never did:
  - `CommandAck` (contract v0, frozen at W0-04) **already carries** `reasonId`, `reasonDetail` and `resultingRulesVersion`, with refinements requiring a rejected ack to carry a reason and an accepted one not to (`packages/sim/contracts/commands.ts`).
  - `contracts/INTERFACES.md` lists CommandAck as the record holding the reason ID, and describes CommittedEvent with typed payload, actor/target IDs and causal parents — no reason field.
  - TP v2.0 §3 makes them separate channels: Worker → main sends `RenderSnapshot`, `CommittedEvent` batches **and** `CommandAck`.
  - TP v1.1 §4 stage 2 emits acknowledgements inside the tick, in authoritative sequence.
  So **`CommandAck` does not enter the committed-event stream**, and no packet has to freeze a "rejection event payload". The real gap is in the lab: a fixture assertion whose `match.reasonId` is set must resolve against the **acknowledgement** stream, which needs (a) the event tape to carry acks alongside events (tape v2) and (b) the matcher to resolve reason matches there. The W0-07 kernel already produces exactly that data — `SimHost.outcomes()` mirrors the CommandAck fields. Proposed home: **P1-12** (public command validator), which is what the kit's own `LAW-NOTICE-REJECTION` note names; it could ride earlier with a lab packet if preferred. Until then the runner keeps reporting those assertions Blocked, which stays correct. **Not a blocker, no contract change.**
- **A tape is hand-authored evidence.** Nothing in the tool distinguishes a truthful tape from a wishful one, which is why provenance (`source`, `producedBy`) is mandatory, the FakeSim watermark is unconditional and `gateEligible` is hard-coded false. W0-07's kernel becomes the first non-human `producedBy`.
- **Recurring process failure, third occurrence (12 Sep):** scripted edits that silently do nothing — the `gates.json` status in W0-11, then twice in P1-01 (a lost `CONTRACT_RECORDS` spread when a later assertion in the same script threw, and a sample manifest written in the wrong format). The rule now: **write the edit, then assert, and never batch an unrelated assertion after a completed write.** Original note: my W0-11 edit to `state/gates.json` silently did nothing — the replacement string assumed 2-space indentation and the file uses 1-space, and I did not assert on it. So the gate record still read `PLANNED` while `STATUS.md` and `GATE_EVIDENCE.md` said `READY_FOR_JANI_APPROVAL`. Found and fixed when recording the approval. The lesson is the one the earlier edits already followed and this one did not: **every scripted edit asserts its match before writing**.
- **CI now runs the browser suite** (12 Sep): the `screens` job executes on the GitHub runner with its own Chromium install and passed on `0f11e88` — shell, worker hash parity, persistence and `/capture`, with captures uploaded as artifacts. Browser evidence no longer depends on this sandbox.
- **W0-07's browser determinism half is now CLOSED** (12 Sep): a browser Worker importing the same `packages/sim/dist` bytes reaches the same authoritative digests as Node — `4d0bd28a` for 137 actors at 600 ticks and `bc38b04a` for 136 at 250 — verified under Playwright in `tests/playwright/worker-hash.spec.ts`.
- **Browser storage quota and eviction are untested** (TP v2.0 §15 names both): a quota check on start and a visible export action belong with the app shell (W0-10+).
- **Frozen kit vs live copies can drift:** intended. Live `design/`, `contracts/` may receive addenda; the frozen copy is the reference for "what was delivered".
