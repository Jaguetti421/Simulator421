# W0-06 — clanlab run, summaries and failure bundles

Phase: **W0 — Web foundation and runtime proof** · Size: **S** · Depends on: W0-05

## Purpose and exact scope
Add `clanlab validate|run|inspect` as a Node CLI with JSON summaries, bounded log files and reproducible failure bundles.

This is one bounded packet. Implement the capability and its verification; do not implement systems mentioned only by its dependencies. If it will not fit one session, checkpoint, write SESSION_RESUME.md and continue in the next session, or split it into numbered children with unchanged aggregate acceptance.

## Read first
AGENTS.md; state/STATUS.md; then: **TP v1.1 §19–21; contracts/FIXTURE_DSL.md (CLI contract)**. "TP v2.0" is The_Last_Clan_Technical_Plan_v2_0_Web.md; "TP v1.1" is reference/Technical_Plan_v1_1.md (authoritative for sections v2.0 does not override). "GDD" is design/GDD_v1.md plus design/GDD_v1_1_Addendum.md.

## Observable acceptance
1. A deliberately failing fixture returns nonzero and writes a bundle that `clanlab inspect` reopens with the failing assertion, tick and evidence paths.
2. Machine output is valid JSON with build/contract/content identity, profile, seed, executed/failed/skipped/blocked counts and artifact paths; detailed logs stay in files.
3. Exit 0 only when every requested supported check passed; BLOCKED and skipped counts are visible.

## Tests and fixtures to add or extend
- packages/lab/cli/*.test.ts

## In-scope paths
- `packages/lab/cli/**`
- `packages/lab/bundle/**`

## Notes
Summary-only stdout; never print logs to the console.

## Return and stop
Commit (push or archive), update state/STATUS.md and state/workboard.json, write handoffs/W0-06/HANDOFF.md from templates/HANDOFF.md, and run the fresh-context self-review (templates/REVIEW.md) before marking ACCEPTED. No unrun check is PASS.
