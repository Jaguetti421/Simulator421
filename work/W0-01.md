# W0-01 — Bootstrap the workspace, CI and instruction files

Phase: **W0 — Web foundation and runtime proof** · Size: **M** · Depends on: none

## Purpose and exact scope
Create the smallest buildable npm workspace with the package skeletons, strict TypeScript, Vitest, ESLint import boundaries, a `clanlab` stub, CI, and the production kit imported under docs/production. Record the actual environment.

This is one bounded packet. Implement the capability and its verification; do not implement systems mentioned only by its dependencies. If it will not fit one session, checkpoint, write SESSION_RESUME.md and continue in the next session, or split it into numbered children with unchanged aggregate acceptance.

## Read first
AGENTS.md; state/STATUS.md; then: **TP v2.0 §2, §21; AGENTS.md; CONVENTIONS.md**. "TP v2.0" is The_Last_Clan_Technical_Plan_v2_0_Web.md; "TP v1.1" is reference/Technical_Plan_v1_1.md (authoritative for sections v2.0 does not override). "GDD" is design/GDD_v1.md plus design/GDD_v1_1_Addendum.md.

## Observable acceptance
1. `npm ci && npm test && npm run lint` pass on a fresh clone; packages sim/content/lab and apps/web exist with README stubs and no game systems.
2. An import of `three`, `react` or a DOM type inside packages/sim fails lint; the failure is demonstrated and recorded.
3. .github/workflows/ci.yml runs build, lint, test, fixture subset and `tools/check_workboard.py`; a Playwright screenshot job exists but may be marked BLOCKED until W0-10.
4. state/STATUS.md records Node/npm/git versions, browser and GPU availability, and whether GitHub push works from the sandbox; state/ACCEPTED_BASELINE.json records the first commit.

## Tests and fixtures to add or extend
- tests/arch/boundaries.test.ts (import boundaries)
- tools/test_tools.py (kit tools)

## In-scope paths
- `package.json`
- `tsconfig*.json`
- `.eslintrc*`
- `packages/*/package.json`
- `apps/web/package.json`
- `.github/workflows/ci.yml`
- `docs/production/**`
- `state/**`
- `README.md`

## Notes
Do not add game logic. Pin versions that actually installed; do not invent versions. If GitHub push works, use it from here on; otherwise produce the archive per AGENTS.md.

## Return and stop
Commit (push or archive), update state/STATUS.md and state/workboard.json, write handoffs/W0-01/HANDOFF.md from templates/HANDOFF.md, and run the fresh-context self-review (templates/REVIEW.md) before marking ACCEPTED. No unrun check is PASS.
