# W0-08 — Persistence smoke: section container, IndexedDB adapter, exactly-once result key

Phase: **W0 — Web foundation and runtime proof** · Size: **M** · Depends on: W0-07

## Purpose and exact scope
Implement the versioned state-section container, a storage interface with in-memory, fake-indexeddb and browser IndexedDB adapters, two-generation durable saves, and a results store with unique-key add semantics.

This is one bounded packet. Implement the capability and its verification; do not implement systems mentioned only by its dependencies. If it will not fit one session, checkpoint, write SESSION_RESUME.md and continue in the next session, or split it into numbered children with unchanged aggregate acceptance.

## Read first
AGENTS.md; state/STATUS.md; then: **TP v2.0 §15; TP v1.1 §15; SAVE-ROUNDTRIP; DATA 01 foundation**. "TP v2.0" is The_Last_Clan_Technical_Plan_v2_0_Web.md; "TP v1.1" is reference/Technical_Plan_v1_1.md (authoritative for sections v2.0 does not override). "GDD" is design/GDD_v1.md plus design/GDD_v1_1_Addendum.md.

## Observable acceptance
1. Snapshot load plus continuation matches the uninterrupted run's hashes for the synthetic kernel (Node adapters); corrupt or truncated generation leaves the previous valid generation intact.
2. Applying the same FinalResult twice increments once; a conflicting payload for the same key fails explicitly; a failed write is never acknowledged as saved.
3. The browser IndexedDB adapter passes the same suite under Playwright (recorded BLOCKED in the sandbox, executed in CI or on Jani's PC).

## Tests and fixtures to add or extend
- packages/sim/persistence/*.test.ts
- tests/fixtures/SAVE-ROUNDTRIP.json
- tests/playwright/persistence.spec.ts

## In-scope paths
- `packages/sim/persistence/**`
- `apps/web/src/persistence/**`
- `tests/playwright/persistence.spec.ts`

## Notes
Export/import JSON can be a stub here; full behavior arrives with P1-33.

## Return and stop
Commit (push or archive), update state/STATUS.md and state/workboard.json, write handoffs/W0-08/HANDOFF.md from templates/HANDOFF.md, and run the fresh-context self-review (templates/REVIEW.md) before marking ACCEPTED. No unrun check is PASS.
