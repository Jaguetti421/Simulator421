/**
 * @lastclan/web — placeholder entry.
 *
 * W0-01 ships the package skeleton only. The Vite app, Worker bootstrap,
 * tabletop camera, procedural identity kit and /capture route arrive in W0-10.
 */
export const APP_NAME = "@lastclan/web" as const;
